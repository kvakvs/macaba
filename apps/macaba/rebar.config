%% -*- mode: erlang; -*-
{erl_opts, [ native
           , {parse_transform, lager_transform}
           ]}.

{sub_dirs, [ ]}.
{deps_dir, "../../deps"}.
{lib_dirs, [ "..", "../../deps" ]}.

{deps, [
       ]}.
